PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.yourmongodb.net/vexorux?retryWrites=true&w=majority
FRONTEND_URL=*
# Generate your own long random value — do NOT reuse an example/default value here.
# e.g. run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SECRET_KEY=REPLACE_WITH_YOUR_OWN_RANDOM_SECRET
